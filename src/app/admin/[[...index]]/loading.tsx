'use client';

/**
 * This route enables showing a loading state right away so that the user knows the studio is loading
 */

import * as Loading from 'next-sanity/studio/loading';

export default Loading;